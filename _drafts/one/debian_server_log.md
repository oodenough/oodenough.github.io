# .what have I done to the debian server

- installed apache2 using apt-get
- installed snap using apt-get
- installed certbot using snap
- mv the ssl self-signed certificate files in `/etc/ssl/https_certificate/`
- installed zsh