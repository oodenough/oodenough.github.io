# sshd

```bash
ssh root@199.247.5.38
scp locale user@ip:remote (-r)
scp usesr@ip:remote locale
```

```bash
sftp user@ip
get remotefile (current_locale_directory)
put local_file_path (current_remote_directory)
```



